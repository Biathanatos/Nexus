<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/main.css">
    <link rel="stylesheet" href="/css/header.css">
    <link rel="stylesheet" href="/css/table.css">
    <link rel="stylesheet" href="/css/form.css">
    <link rel="stylesheet" href="/css/service_detail.css">
    <link rel="stylesheet" href="/css/markdown_viewer.css">
    <title>SERVICES | <%= service.name %></title>
</head>
<body>
    <header>
        <h1>Create a New Service</h1>
        <ul id="nav">
            <li><a href="/services">Services</a></li>
            <li><a href="/">Nexus</a></li>
            <li><a href="/user/logout">Logout</a></li>
            <li><a href="/user/profile">Profile</a></li>
        </ul>
    </header>
    <main class="detail-container">
        <section class="detail-hero">
            <div class="detail-title">
                <h1 id="service-name"><%= service.name %></h1>
                <p class="detail-description"><%= service.description %></p>
                <div class="detail-meta">
                    <span class="detail-meta-item">Créé le <%= service.created_at %></span>
                    <% if (isAI) { %>
                        <span class="detail-meta-item detail-meta-item--success">Accessible par IA</span>
                    <% } else { %>
                        <span class="detail-meta-item">Non accessible par IA</span>
                    <% } %>
                </div>
            </div>
            <div id="service-tags" class="detail-tags">
                <% if (service.tags && service.tags.length > 0) { %>
                    <% for (const tag of service.tags) { %>
                        <span class="detail-tag"><%= tag %></span>
                    <% } %>
                <% } else { %>
                    <span class="detail-tag detail-tag--muted">Aucun tag</span>
                <% } %>
            </div>
        </section>

        <section class="detail-panel">
            <div id="service-content" class="detail-service-content"></div>
        </section>
    </main>
    <% if (clientScriptUrl !== '#') { %>
    <script type="module" src="<%= clientScriptUrl %>"></script>
    <script src="/js/services_detail.js"></script>
    <% } else { %>
    <script id="detail-script" src="/js/services_detail_test.js"></script>
    <% } %>
</body>
</html>